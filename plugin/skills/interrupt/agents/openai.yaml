interface:
  display_name: "Claude Code Interrupt"
  short_description: "Gracefully interrupt a Claude Code job"
  default_prompt: "Use $cc:interrupt to stop a Claude Code job while preserving its session."
