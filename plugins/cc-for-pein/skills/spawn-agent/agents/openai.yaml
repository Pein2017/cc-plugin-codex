interface:
  display_name: "CC Spawn Agent"
  short_description: "Experimental: start an asynchronous durable Claude Agent"
  default_prompt: "Experimental: use $cc-for-pein:spawn-agent with fork_turns=none and an explicit model. Sonnet/Opus are for general work; Haiku 4.5 at low effort is test-only for smoke, hook, environment, and integration checks. Stop further real CC tests on explicit account-limit exhaustion. Classify join policy first, because completion cannot reactivate an idle Codex parent."
