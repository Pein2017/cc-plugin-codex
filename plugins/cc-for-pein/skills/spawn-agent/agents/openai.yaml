interface:
  display_name: "CC Spawn Agent"
  short_description: "Experimental: start an asynchronous durable Claude Agent"
  default_prompt: "Experimental: use $cc-for-pein:spawn-agent and invoke mcp__cc_for_pein__spawn_agent with a self-contained message, exact model, and explicit write intent; default to leaf, and use claude_orchestrator only with exact Fable when native subagents are deliberately required. Never fall back to a shell command, and remember that completion cannot reactivate an idle Codex parent."
