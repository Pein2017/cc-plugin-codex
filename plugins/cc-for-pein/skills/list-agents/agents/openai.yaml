interface:
  display_name: "CC List Agents"
  short_description: "Experimental: list current-root durable Claude Agents"
  default_prompt: "Experimental: use $cc-for-pein:list-agents and invoke mcp__cc_for_pein__list_agents only for current-root state; never fall back to a shell command. Listing is not progress and cannot reactivate an idle Codex parent. Never call it solely to recheck completion after a quiet wait_agent timeout; wait_agent again directly instead."
