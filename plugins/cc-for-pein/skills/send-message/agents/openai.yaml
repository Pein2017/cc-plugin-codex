interface:
  display_name: "CC Send Message"
  short_description: "Experimental: deliver or queue a durable Agent message"
  default_prompt: "Experimental: use $cc-for-pein:send-message and invoke mcp__cc_for_pein__send_message to deliver or queue a durable Agent message; never fall back to a shell command. Summarize success in one concise disposition-aware sentence, never raw JSON or repeated message text unless debug detail was explicitly requested. Queueing cannot reactivate an idle Codex parent."
