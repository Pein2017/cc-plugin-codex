interface:
  display_name: "CC Interrupt Agent"
  short_description: "Experimental: interrupt a Claude Agent current turn"
  default_prompt: "Experimental: use $codex-harnessdock:interrupt-agent and invoke mcp__codex_harnessdock__interrupt_agent to stop a Claude Agent turn while preserving safe continuation; summarize success without raw JSON and never fall back to shell. It cannot reactivate an idle Codex parent."
