## 1. Freeze Evidence And Closed Shapes

- [x] 1.1 Record the sanitized Haiku/low stream-json terminal-result key/type witness from the fixed terminal-parity environment, including the exact admitted numeric fields and the rejected nested/string/array fields, without retaining prompt, response, credentials, session identity, or raw event content.
- [x] 1.2 Add a pure version-one metrics normalizer and Harness-neutral validator for nullable provider-reported and Plugin-observed metrics, including safe-integer, finite, non-negative, partial, malformed, and no-evidence cases.
- [x] 1.3 Add one shared Agent Card projection helper with closed authority/phase values, nullable retained-job evidence, query-time elapsed seconds, and no liveness, attention, summary, path, command, or mailbox inference.

## 2. Propagate Terminal Metrics End To End

- [x] 2.1 Extract only the admitted terminal `result` numeric fields in the Claude stream parser, preserve partial metrics on terminal errors, and discard raw/unknown usage payloads from public projection.
- [x] 2.2 Carry validated metrics through the Claude Driver result, supervisor aggregate, attempt/recovery counters, and terminal job result without changing failure, final-message, exact-session, steering, or reconnect semantics.
- [x] 2.3 Add nullable metrics to normalized completion facts, structural equality, first-delivery freezing, root-wide wait, targeted barrier entries, pruning survival, and byte-identical redelivery.
- [x] 2.4 Preserve legacy jobs, legacy inboxes, and pre-change frozen payloads as `metrics: null` without backfill, mutation, acknowledgement changes, or new durable schema ownership.

## 3. Expose Compact Agent Cards

- [x] 3.1 Extend spawn and list projections with the approved Agent Card fields while preserving their existing lifecycle-key names, exact root scoping, concise presentation, and operator-only internals.
- [x] 3.2 Derive safe phase/activity only from admitted public progress, hide hook/unknown activity, and prove listing neither claims the single-progress budget nor reads or acknowledges completion.
- [x] 3.3 Add focused card tests for active, terminal, pruned-detail, missing-effort, unknown-authority, elapsed-time, private-hook, and repeated-list cases.

## 4. Tighten Model-Facing Guidance And Documentation

- [x] 4.1 Update typed MCP receipt tests, Skills, and presentation rules for Agent Card and optional metrics while keeping seven operations, unchanged input schemas, dynamic lead-owned join choice, and no JSON dump by default.
- [x] 4.2 Move shared asynchronous/join policy to concise server instructions, reduce tool descriptions to operation-specific differences, and enforce semantic markers plus a smaller combined instruction budget.
- [x] 4.3 Correct the README version and wait comparison for 0.14 targeted single-turn/all-settled joins, event wakeup, Agent Card authority wording, and provider-reported-not-billed cost semantics.

## 5. Verify The Fixed Implementation

- [x] 5.1 Add adapter, Driver-contract, supervisor/job, completion-inbox, Agent runtime, MCP, plugin-contract, and fake-Claude integration coverage for every new and legacy scenario.
- [x] 5.2 Run focused tests during implementation, then `openspec validate improve-agent-card-and-usage-receipts --strict`, `openspec validate --all --strict`, `git diff --check`, and `npm run check` from the developer worktree.
- [x] 5.3 Freeze the exact developer tree and run one independent Sol/high read-only audit for metric provenance, payload leakage, frozen-redelivery compatibility, progress-budget mutation, elapsed/liveness confusion, and receipt/schema drift.
- [x] 5.4 Let the Codex lead disposition every finding, apply at most one bounded builder correction when local, rerun affected focused/full gates, and record the route result without changing model priors from one task.
- [x] 5.5 Leave the accepted result on `developer` with no promotion, install, refresh, release, archive, or remote push unless separately authorized.

Verification disposition: Luna/medium scouting was accepted first-pass; Terra/high implementation was accepted after one bounded correction; independent Sol/high verification held on concrete P1 findings and then passed the corrected tree with no remaining P0/P1. This task-local receipt does not change the standing routing priors.
